// Generated by CoffeeScript 1.8.0
var Client, Config, Contact, CozyInstance, WebDavAccount, async, getImports;

Contact = require('../models/contact');

Config = require('../models/config');

CozyInstance = require('../models/cozy_instance');

WebDavAccount = require('../models/webdavaccount');

async = require('async');

Client = require('request-json').JsonClient;

getImports = function(callback) {
  return async.parallel([
    function(cb) {
      return Contact.request('all', cb);
    }, Config.getInstance, CozyInstance.first, function(cb) {
      var dataSystem;
      dataSystem = new Client("http://localhost:9101/");
      return dataSystem.get('tags', function(err, response, body) {
        return cb(err, body);
      });
    }, WebDavAccount.first
  ], function(err, results) {
    var config, contacts, instance, locale, tags, webDavAccount;
    contacts = results[0], config = results[1], instance = results[2], tags = results[3], webDavAccount = results[4];
    locale = (instance != null ? instance.locale : void 0) || 'en';
    if (webDavAccount != null) {
      webDavAccount.domain = (instance != null ? instance.domain : void 0) || '';
    }
    return callback(null, "window.config = " + (JSON.stringify(config)) + ";\nwindow.locale = \"" + locale + "\";\nwindow.initcontacts = " + (JSON.stringify(contacts)) + ";\nwindow.tags = " + (JSON.stringify(tags)) + ";\nwindow.webDavAccount = " + (JSON.stringify(webDavAccount)) + ";");
  });
};

module.exports = {
  index: function(req, res) {
    return getImports(function(err, imports) {
      if (err) {
        return res.error(500, 'An error occured', err);
      }
      return res.render('index.js', {
        imports: imports
      });
    });
  },
  widget: function(req, res) {
    return getImports(function(err, imports) {
      if (err) {
        return res.error(500, 'An error occured', err);
      }
      return res.render('widget.jade', {
        imports: imports
      });
    });
  },
  setConfig: function(req, res) {
    return Config.set(req.body, function(err, config) {
      if (err) {
        return res.error(500, 'An error occured', err);
      }
      return res.send(config);
    });
  }
};
