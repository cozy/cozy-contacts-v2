if hasPicture
  img(src="contacts/#{id}/picture.png?#{timestamp}")
else
  img(src="img/defaultpicture.png")

h2 #{displayName}
div.infos      
  ul.tags
    for tag in tags || []
      li.tag(style="background: #{tag.get('color')};") #{tag.get('name')}
  
  span.email #{bestmail}
  span.tel   #{besttel}
.clearfix
