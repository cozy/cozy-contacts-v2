// Generated by CoffeeScript 1.10.0
var Realtimer, americano, application;

americano = require('americano');

Realtimer = require('cozy-realtime-adapter');

module.exports = application = function(options, callback) {
  if (options == null) {
    options = {};
  }
  if (callback == null) {
    callback = function() {};
  }
  options.name = 'Contacts';
  if (options.root == null) {
    options.root = __dirname;
  }
  if (options.port == null) {
    options.port = process.env.PORT || 9114;
  }
  if (options.host == null) {
    options.host = process.env.HOST || '127.0.0.1';
  }
  return americano.start(options, function(err, app, server) {
    var realtime;
    realtime = Realtimer(server, ['contact.*', 'tag.*']);
    return typeof callback === "function" ? callback(null, app, server) : void 0;
  });
};

if (!module.parent) {
  application();
}
